-- Enable Row Level Security for auth.users if not enabled (this is typically already enabled)
-- We don't need to create additional tables since we're using a predefined user

-- This migration prepares the database for authentication
-- The user will be created manually through Supabase dashboard or during first signup